namespace Model.DotNetCore.Enums
{
    // dummy commit for testing nuget. #1
    public enum RepeatType : byte
    {
        Never = 0,
        Daily = 1,
        WeekDays = 2,
        Weekly = 3,
        Monthly = 4,
        Yearly = 5
    }
}